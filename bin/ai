#!/usr/bin/ruby
if File.exists?(File.join(File.dirname(__FILE__),"..","lib","aineko.rb"))
  # set up load path in dev env
  aipath = File.join(File.dirname(__FILE__),"..","lib")
  $LOAD_PATH<<aipath if !$LOAD_PATH.include?(aipath)
else
  require 'rubygems'
end
require 'aineko'

Aineko::Runner.new.__run__($*)

